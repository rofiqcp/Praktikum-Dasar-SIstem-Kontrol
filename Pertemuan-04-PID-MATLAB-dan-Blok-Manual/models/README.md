# Simulink models
Run `../examples/build_pid_manual_simulink.m` in MATLAB. It creates `pid_manual_water_heater.slx` in this folder. The builder is the source-of-truth so the binary model can always be regenerated.
